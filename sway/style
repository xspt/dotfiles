include vars

output * bg $mantle solid_color

gaps inner 10

font "Mononoki Nerd Font" 11

default_border pixel 2
client.focused $blue $base $text $yellow $blue
client.focused_inactive $clear $base $text $clear $clear
client.unfocused $clear $base $text $clear $clear
client.urgent $red $base $text $pink $red
